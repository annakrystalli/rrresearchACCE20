# `mypackage` `DESCRIPTION`

The `DESCRIPTION` file contains metadata about your package, including dependencies

It should look a bit like this:


Your script should contain:

```{r, eval=FALSE, code=readLines("mypackage-DESCRIPTION")}

```